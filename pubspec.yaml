name: super_hero_word_game
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15

  word_search:
    git:
      url: https://github.com/ShakyaCsun/word_search.dart.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - images/
